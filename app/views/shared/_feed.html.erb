<% if @feed_items.any? %>
  <ol class="microposts">
    <!-- @feed_items defined previously for use in the profile -->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>